# ======================
# Database (MySQL 8+)
# ======================
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=new@123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# (Optional, Hibernate will auto-detect)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ======================
# JWT
# ======================
jwt.secret=VGhpcyBpcyBhIHNlY3VyZSBzZWNyZXQga2V5IGZvciBKV1QgYXV0aA==
jwt.expiration=3600000
jwt.refresh-expiration=604800000

# ======================
# Mail (Gmail, STARTTLS on 587)
# Fixes SMTPSendFailedException: [EOF]
# ======================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=seprojecttest.23@gmail.com
spring.mail.password=yyngbzcshoqspdeu   # app password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
# Helpful during debugging
spring.mail.test-connection=true
logging.level.org.eclipse.angus.mail=DEBUG

# ======================
# App
# ======================
app.base-url=http://localhost:8080
# server.port=8080  # uncomment to change

# ======================
# CORS ? allow FE dev + auth headers
# ======================
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type,Refresh-Token
spring.web.cors.exposed-headers=Authorization
